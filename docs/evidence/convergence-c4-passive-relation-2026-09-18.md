# Convergence C4 Passive Relation — qualification 2026-09-18

Status: **PASS / BOUNDED PASSIVE WORLD↔DYNAMIC RELATION**

Qualified source:

`1e327b2e1ba8f68f6494937431696576eddaf3a3`

Workflow:

`35367211196`

## Claim

C4 asked:

> can the lineage-owned structural seam that drove C2/C3 remain meaningful after authority composition and produce one passive rotational DOF, with ordinary gravity as the driver?

C4 deliberately excludes motors, angular limits, relation history/succession and generic mechanical graphs.

## A/B experiment

Two otherwise equivalent C3 specimens are created.

Control:

- same materially-continuous six-cell WORLD Matter;
- same C2 seam-derived rigid split;
- same C3 authority composition;
- no passive relation host.

Challenger:

- same C2/C3 chain;
- relation truth is only two retained lineage endpoint tokens + seam frame/axis;
- no body/provider/Space identity is stored in relation truth;
- after C3 transfer, lineage ownership resolves current authorities;
- one disposable `HingeJoint3D` hosts the dynamic side against a fixed static WORLD endpoint;
- no motor;
- no angular limits;
- gravity is the only driver.

Collision is disabled for the two dynamic test islands so contact cannot explain the A/B difference.

## Result

Metric:

`control_anchor_gap=32.489716 control_rotation=0.000000 related_anchor_gap=0.003385 related_rotation=4.173177 related_min_y=8.000000 control_min_y=-24.489717 world_token=42003 island_token=42004`

Interpretation:

- unjointed C3 control falls freely and does not spontaneously rotate;
- related island remains tightly anchored at the authored seam;
- related island develops large solver-owned rotation under ordinary gravity;
- island Matter and lineage remain unchanged by the relation motion;
- both lineage endpoints remain live on their respective authorities;
- relation metadata is independent of current body/provider identity.

## Bounded host limitation

For this first C4 specimen the anchored side is canonical static WORLD.

Godot Joint3D semantics allow one endpoint to be omitted, in which case the other body is attached to an implicit fixed StaticBody3D. C4 uses that as disposable solver machinery.

Therefore C4 does **not** yet establish arbitrary dynamic↔dynamic relations.

## Protected chain

The same branch re-passed:

- `C2_STRUCTURAL_LAW_PASS`;
- `C3_AUTHORITY_COMPOSITION_PASS`;
- `W0D_RECOVERY_CAUSAL_LOOP_PASS`;
- exact exported `C0_EXPORTED_SPARK_BASELINE_PASS`.

Exact C4 executable SHA-256:

`252552b0b76f5cd8efd169d7a9634f658b2af61a7328e6c1ec50579d1b9ccb56`

## Earned conclusion

Within the bounded WORLD↔dynamic specimen:

> **a local lineage-owned structural seam can survive authority reorganization as a passive physical relation whose motion is produced by world forces rather than a mechanism launch command.**

This is not yet a living-world mechanism.

## Next gate

C5 — Living World.

First pressure should be history that does not require inventing a reconnect system:

1. edit non-anchor Matter while the relation is live;
2. destroy one relation-owning Matter endpoint and require the host relation to die;
3. recreate Matter at the same coordinate with fresh lineage and require the old relation to remain dead;
4. then pressure topology succession/reframing if the relation still deserves promotion.

Rigid bypass/reconnect pressure remains valuable but should not be smuggled into C5 before cross-authority reconnect semantics exist.

# Convergence C6 Autonomous Qualification — 2026-09-18

Status: **PASS / EXACT OWNER ARTIFACT QUALIFIED**

Qualified source:

`748c1e413c9a89b192f9117e78eb76091d7d49f1`

Workflow run:

`35382830457`

Job:

`105722963863`

## Question

Can the earned C2→C5 causal language operate through the real Owner-facing scene and actual input path as one continuous packaged-world flow, rather than only in source/headless fixtures?

## Real-scene result

The production scene `res://p1/recovery_main.tscn` passed:

> ordinary WORLD Matter → local structural law → rigid decomposition → authority composition → same-frame passive relation → gravity-driven motion → actor ride → live moving edit → endpoint-history death without same-address resurrection.

Runtime metric:

`selected=36 install_frame=40 immediate_gap=0.0000000000 max_gap=0.012545 rotation=0.743121 actor_ride_frames=80 old_endpoint=1202092 fresh_endpoint=1203912`

## Production pointer / input result

Rendered Forward+/D3D12 evidence used the real production pointer resolver and the persisted `H` InputMap action.

Metric:

`screen=(418.0,381.0) norm=(0.4066,0.5292) candidates=1 selected=36 relation_live=true`

The first rendered attempt exposed a test-contract race: after real `H` input, the authority transaction could legally commit before the following `process_frame`. The test had incorrectly required the intermediate `pending` state to remain observable.

The contract was corrected to accept either:

- transaction still pending; or
- the same transaction already atomically committed with active relation truth.

Runtime was not delayed to satisfy the test.

## Exact exported executable authority

The exact Windows release executable itself then ran its own rendered autonomous qualifier using:

- production scene;
- production pointer resolver;
- OS pointer alignment;
- real `Input.parse_input_event` for `H`;
- real W0 authority transaction;
- real passive physics relation;
- actor support handoff;
- moving Matter edit/history;
- rendered screenshots.

Exact executable metric:

`screen=(421.0,355.0) selected=36 install_frame=225 immediate_gap=0.0000000000 max_gap=0.012545 rotation=0.715676 ride_frames=80 old_endpoint=1202092 fresh_endpoint=1203912`

Marker:

`C6_EXPORTED_AUTONOMOUS_FLOW_PASS`

The same exact executable separately re-passed the protected C0 Spark:

`target_fall=0.415761 actor_fall=0.415761 handoff_error=0.00000000 active_spaces=2`

Marker:

`C0_EXPORTED_SPARK_BASELINE_PASS`

## Artifact identity

Executable SHA-256:

`f615d31bf3028cbaf023ac878a7c522b3ff172e113ffc1ee95f9bf31e3b11ec7`

Owner artifact:

- name: `FrameMatter-Convergence-C6-Windows`
- artifact id: `10562783825`
- workflow artifact digest: `sha256:b51f1f9955e13ecd85c139e7dc3e962a2cd58a7c0514e6a97af2babb1faeb271`

Rendered evidence artifact:

- name: `FrameMatter-Convergence-C6-Rendered-Evidence`
- artifact id: `10562843623`
- digest: `sha256:d06f240b1fd58f4f4e52ccaa6e98ee9750582947c1086d91a7beb7e9a26fd798`

## Earned conclusion

C6 is PASS.

The exact packaged Owner scene, not merely a source probe, reproduces the intended causal chain through the real interaction surface.

This closes the technical uncertainty that C6 was designed to remove.

## Deliberate limits

C6 does **not** claim:

- final relation ontology;
- reconnect / authority merge;
- generic mechanism framework;
- real fine-resolution Matter representation;
- solved embodiment/contact asymmetry;
- polished structural-seam presentation;
- broad expressiveness proven by Owner play.

Those remain outside C6.

## Next authority

Do not rebuild or polish before the next evidence boundary.

The already-qualified executable above is the C7 Owner Spark Gate specimen.

C7 asks an experiential question only:

> does one new world-law primitive provoke spontaneous construction pressure and reveal the next missing primitive through play?

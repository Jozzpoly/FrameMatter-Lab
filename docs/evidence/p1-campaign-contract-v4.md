# P1 campaign contract v4 — Owner visual falsification response

Status: **OWNER-AUTHORIZED ACCEPTANCE STRENGTHENING**

Trigger: direct Owner test of frozen runtime `b5050b669ec4101226095183929f5790f0a034fc` plus frame-by-frame review of the Owner recording.

Contract v3 successfully prevented delivery without exact-candidate evidence, adversarial rehearsal and independent review. The Owner test nevertheless exposed a missing acceptance dimension: the rendered surface could satisfy the curated scenario set while failing under accumulated freeform geometry and while the base Matter surface itself was not independently validated without semantic overlays.

Version 4 therefore **adds** acceptance pressure; it removes or relaxes nothing.

## Additions

### New required gate: `base_matter_surface_integrity`

This gate requires direct rendered evidence that base Matter, with semantic overlays disabled, reads as solid, opaque and coherent three-dimensional mass. It exists to prevent grid/state/target presentation from compensating for or hiding a broken physical-form renderer.

### New representative scenario: `chaotic_edit_visual_stress`

This scenario requires accumulated irregular editing rather than a short reversible scripted burst. The geometry must develop holes, concavities, steps/protrusions and enough local edge density to expose renderer and presentation failure under realistic Owner behavior.

The exact test shape may be deterministic for reproducibility, but it must preserve the **accumulated geometry entropy** characteristic that was absent from the prior G8 rehearsal.

### Expanded negative baseline

`docs/evidence/p1-owner-test-visual-failure-and-rebuild.md` is added as a second-generation negative Owner baseline alongside the earlier failed-candidate records.

## What remains protected

All inherited owner goals, protected substrate invariants, representative scenarios and gates remain required. This contract does not reinterpret the direct Owner failure as permission to weaken mechanical, interaction, camera, assurance or delivery requirements.

## Promotion consequence

The delivered `b5050b669...` runtime remains durable historical evidence but is no longer an approved Owner candidate. A future candidate must re-earn all required gates/scenarios, including the new base-surface and chaotic-edit requirements, on one exact runtime before Owner delivery.
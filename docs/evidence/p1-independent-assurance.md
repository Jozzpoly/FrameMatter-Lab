# P1 independent assurance — PASS

## Verdict

**PASS** for frozen runtime `b5050b669ec4101226095183929f5790f0a034fc`.

This record preserves the result returned by the separate Browser ChatGPT read-only falsification context `p1-ia-browser-20260915T2321+0200-b5050b66`. The reviewer reported zero material findings, confirmed that the runtime remained frozen, and confirmed that no repository or candidate changes were authored during review.

Machine-readable authority: `quality/assurance/p1-independent-review.json`.

## Owner-goal / evidence fit

The reviewer judged that the recovery claim did not collapse into mechanical correctness plus attractive screenshots. Candidate-native rendered and temporal evidence directly exposes the Owner-visible research surface: Matter form and granularity, STATIC/DYNAMIC state, actor relations, editing intent/result, finite motion, topology succession, camera context and UI hierarchy.

The review specifically judged the historical P1 failure classes to be materially prevented rather than merely bypassed by deterministic fixtures: lighting collapse, missing cell-scale language, through-wall debug targeting, telemetry-dominated HUD, weak state semantics, weak world-motion reference and catastrophic close-camera composition.

## Evidence depth

The reviewer inspected the mandatory contract/governance records, negative baseline, candidate-native G4/G5/G6/UI/cross-layer/general rendered artifacts, exact-candidate foundation/readiness provenance, both the rejected and accepted G8 artifacts, both G8 harness versions, the accepted exact-checkout workflow policy, all 12 accepted G8 checkpoints and the full 212-frame continuous G8 movie.

Nominal review included default static Matter, REMOVE/PLACE/EXPAND feedback, zero-launch release followed by explicit finite motion, moving edit plus non-zero storage rebase, topology succession and freeze-at-current-pose with mixed successor state.

Off-nominal review included real close-Matter camera obstruction, edge/minimum-zoom pressure, far-airborne composition, production automatic fall/recovery, topology succession after prior motion and rebase, the rejected first G8 run, the accepted retry, the test-only relocation transient and the recovery-boundary context-loss frames.

## G8 retry judgement

The independent reviewer accepted the second G8 run as a legitimate evidence-stimulus correction rather than threshold relaxation. The original `> 0.75 m` material-independence criterion remained unchanged; the accepted harness repeated the same legal production finite impulse three times and extended only the bounded observation window. Frozen runtime bytes remained exact.

## Non-material residual risks

The reviewer retained three residual observations rather than hiding them:

- a few frames immediately before automatic fall recovery approach the limit of remote-context readability, but do not become sustained failure and recover without oscillation;
- arbitrary instantaneous teleport camera continuity is not a defended claim, and the G8 fixture relocation provides a useful negative seed if that ever becomes a production mechanic;
- the 3.53 s G8 movie is deliberately compressed evidence, not by itself a UX demonstration; it is sufficient only together with the staged candidate-native rendered evidence.

The reviewer also identified stale historical wording in `docs/p1-owner-facing-recovery-campaign.md` as non-material governance documentation drift and explicitly requested reconciliation during final readiness audit.

## Assurance boundary

This independent PASS does **not** itself authorize Owner delivery. The reviewer explicitly left `final_readiness_audit` as a separate required gate. Any runtime-affecting change after the reviewed frozen SHA invalidates this assurance and requires a new candidate/review cycle.

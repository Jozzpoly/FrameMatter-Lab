# FrameMatter Convergence — adaptive campaign map

Status: **active decision map**. Sequence is provisional; gates and stop rules matter more than labels.

## Campaign objective

Move from the recovered Spark to a small but genuinely expressive living-material sandbox in which the Owner can discover what the next world primitive should be through play.

The campaign deliberately separates:

- **technical truth** — can the world rule actually happen?
- **experiential truth** — does it create the intended building/physical intuition?
- **ontological commitment** — has repeated pressure earned a reusable abstraction?

Never promote one into another silently.

---

## C0 — exact-artifact Spark baseline

**Status: PASS / frozen**

Qualified source:

`028c12764692b8d79ba9b2c634ed9fbd6a2d7f70`

Required causal loop in the exported release executable:

> ordinary WORLD Matter cut → authority transfer → derived dynamic autonomy → exact actor/support handoff → gravity/ride → moving Matter remains editable.

Material finding discovered by C0:

Critical W0 staging mutations had been hidden inside `assert(...)`; debug probes passed while release exports skipped the mutations. The transaction was made release-safe and requalified.

C0 is the protected baseline for later Convergence builds.

---

## C1 — relative scale / authoring probe

**Status: ACTIVE**

Question:

> at which relative cell scale, if any, does the Owner start thinking “I can build things here” rather than “I am placing giant blocks”?

Current proxy modes in one runtime:

- 1x ≈ 100 cm relative cell
- 2x ≈ 50 cm
- 4x ≈ 25 cm
- 8x ≈ 12.5 cm

This is a perception/authoring proxy only. Matter storage remains the same 1-cell backend.

Technical gate before Owner test:

- scale switching is lifecycle-safe;
- actor/camera/reach scale coherently;
- no engine/script errors during repeated reset;
- exact C1 release executable still passes C0 Spark qualification.

Owner gate:

- compare variants quickly;
- identify first useful qualitative transition, if any;
- report whether finer scale meaningfully changes building instinct.

Stop rules:

- if even 8x does not materially improve the instinct, do not automatically pursue smaller cells;
- investigate interaction grammar/world language as the stronger blocker;
- do not implement adaptive representation before a useful target scale exists.

---

## C2 — structural law: adjacency != rigid connectivity

**Status: QUEUED AFTER C1 DECISION**

Question:

> can identical occupied Matter geometry resolve into either one rigid island or multiple mechanically related islands because of one local world rule?

Build the smallest possible experiment.

Required evidence:

- same Matter occupancy in control and challenger;
- control behaves as one rigid connected organization;
- one bounded local law changes the derived physical organization;
- no special pre-authored “moving object” is required;
- destroying/changing the relevant material relation changes the consequence coherently;
- Matter identity is not redefined as joint/body identity.

Stop rule:

Do not design a universal interface/constraint framework. One falsifiable relation is enough.

---

## C3 — rotational seam / Bearing specimen

**Status: CONDITIONAL**

Only if C2 supports the structural-law hypothesis.

Challenge:

> two adjacent material regions remain materially situated together while permitting one meaningful rotational degree of freedom.

Bearing is an experimental specimen, not the ontology.

Compare at least:

- constrained relation present;
- relation absent/rigid;
- relation destroyed or materially invalidated.

Observe whether the Owner can predict what will happen from the world itself.

---

## C4 — living edit through mechanism

**Status: CONDITIONAL**

Pressure the C3 relation after creation:

- edit Matter on either side;
- remove supporting/owning Matter;
- alter geometry around the interface;
- move/ride the mechanism;
- verify no magical resurrection after destroy/recreate-at-same-address.

Question:

> does the mechanism remain a living consequence of Matter and history, or become an invisible object-system bolted onto the world?

---

## C5 — first purposeful construction

**Status: CONDITIONAL / OWNER-LED**

Create a tiny authored situation where the new world language enables a useful construction rather than a lab demonstration.

Examples are not commitments: door, pivoting bridge, wheel-like support, lever, simple suspension, counterweight.

The Owner should be able to invent or modify the construction, not merely trigger it.

Question:

> did the primitive increase the space of things worth building?

This gate chooses whether the primitive deserves further generalization.

---

## C6 — direct interaction fidelity

**Status: CONDITIONAL**

Re-test the strongest loop through real Owner input rather than test hooks:

> point/act → edit → topology/structural consequence → move/ride → edit again.

Qualify pointer targeting, UI ownership, rejection feedback and causal readability at the scale/primitive selected by C1–C5.

Do not confuse source-level mutation authority with real interaction-path evidence.

---

## C7 — convergence checkpoint

**Status: FUTURE**

Synthesize Owner play + technical evidence and decide the next research branch.

Possible outcomes include:

- deepen WORLD LANGUAGE;
- real finer-resolution representation;
- embodiment/work;
- actor/contact mechanics;
- world scale/streaming;
- another primitive.

C7 must not choose by roadmap inertia. The strongest repeated pressure wins.

---

## Cross-cutting rules

Every C-stage should preserve C0 unless the experiment explicitly challenges a protected assumption.

A stage may be skipped, reordered or terminated when evidence makes its question obsolete.

Do not accumulate framework debt ahead of repeated need.

Do not spend Owner attention on a build that still has unresolved technical errors detectable in CI.

Do not call a technical proxy an experiential result.

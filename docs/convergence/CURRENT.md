# FrameMatter Convergence — CURRENT

Status: **live mutable execution pointer**

Updated: 2026-09-18

This file answers only four questions:

1. what is the active research objective?
2. what is proven now?
3. what is currently broken/open?
4. what is the next legitimate move?

If live repository/CI evidence contradicts this file, live evidence wins and this file must be corrected.

## Active objective

Qualify **C1 relative scale / authoring probe** without weakening the protected Spark.

The immediate decision we want is not “what should final voxel resolution be?”

It is:

> does a materially smaller apparent Matter unit create a qualitative jump in the Owner's desire/ability to construct mechanisms and shapes?

## Frozen protected baseline

C0 branch:

`research/convergence-c0-baseline`

Frozen qualified C0 source:

`028c12764692b8d79ba9b2c634ed9fbd6a2d7f70`

Exact exported release result:

`C0_EXPORTED_SPARK_BASELINE_PASS`

Protected properties:

- ordinary visible terrain is Matter;
- direct destructive topology can derive dynamic autonomy;
- retained Matter lineage transfers rather than clones;
- actor/support handoff is world-continuous;
- detached Matter moves under physics;
- actor rides it;
- moving Matter remains editable.

## Active branch

`research/convergence-c1-scale-probe`

C1 currently provides one runtime with 1x / 2x / 4x / 8x relative-scale proxy modes.

## Current technical boundary

First C1 qualification showed:

- C0 source-level Spark still PASS;
- C1 scale values themselves reached expected values;
- reset exposed script errors from typed references to retired/freed `LocalMatterSpace` instances.

Failure classification:

**PRODUCT DEFECT / lifecycle reset boundary**, not scale-hypothesis failure.

The active fix is to treat world replacement as an explicit lifecycle transaction:

- clear actor support before old world retirement;
- clear focus/camera references before registry publication;
- reacquire observer canonical-WORLD identity after reset;
- then re-run C1 smoke and exact-release C0 gate.

## Next legitimate move

1. finish and validate lifecycle-safe C1 reset;
2. require clean C1 smoke with no engine/script failure markers;
3. export exact C1 Windows artifact;
4. run C0 exact-artifact qualifier inside that C1 executable;
5. only after GREEN, produce the bounded Owner C1 package;
6. ask the Owner only the scale/authoring-feel question;
7. record the result here before entering C2.

## Owner boundary

The Owner is **not** needed to debug C1.

The next Owner input becomes uniquely valuable only after technical qualification, when comparing 1x/2x/4x/8x construction feel.

## Explicitly not next

Until C1 evidence says otherwise, do not:

- implement final 25 cm Matter;
- build octrees/VDB/adaptive LOD;
- begin Bearing implementation;
- redesign the full actor;
- reopen P1 visual-governance work;
- optimize performance without demonstrated C1 pressure;
- merge PR #1 to main.
